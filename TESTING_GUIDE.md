# 快速測試指南

## 🚀 啟動應用

Metro Bundler 已經在運行中。請按照以下步驟測試：

### 1. 掃描 QR Code
在終端中應該會看到 QR Code，使用以下方式掃描：
- **iOS**: 使用 Expo Go App 掃描
- **Android**: 使用 Expo Go App 掃描

### 2. 或使用模擬器
在終端按下：
- `a` - 在 Android 模擬器中打開
- `i` - 在 iOS 模擬器中打開
- `w` - 在網頁瀏覽器中打開

---

## ✅ 測試檢查清單

### 第一次啟動測試
- [ ] App 啟動後顯示歡迎頁面（login.tsx）
- [ ] 可以輸入暱稱（最多 10 字）
- [ ] 可以選擇 8 種顏色之一
- [ ] 點擊「開始使用」後進入主頁
- [ ] 主頁顯示「還沒有計畫」的空狀態

### 創建計畫測試
- [ ] 點擊「創建計畫」按鈕
- [ ] Modal 彈出顯示表單
- [ ] 可以輸入計畫名稱
- [ ] 可以輸入目的地
- [ ] 可以設定密碼
- [ ] 點擊「創建」後顯示計畫 ID
- [ ] 可以複製計畫 ID
- [ ] 計畫出現在列表中
- [ ] 顯示參與者數量（1 人）
- [ ] 顯示參與者顏色點

### 個人資料測試
- [ ] 進入個人資料頁面
- [ ] 顯示暱稱和顏色
- [ ] 顯示裝置 ID（部分）
- [ ] 點擊「編輯個人資料」
- [ ] Modal 彈出顯示編輯表單
- [ ] 可以修改暱稱
- [ ] 可以選擇新顏色
- [ ] 點擊「儲存」後更新成功
- [ ] 返回後看到更新的資料

### 加入計畫測試（需要第二台設備或清除資料）
- [ ] 清除 App 資料或使用另一台設備
- [ ] 重新設定暱稱和顏色
- [ ] 點擊「加入計畫」按鈕
- [ ] 輸入之前複製的計畫 ID
- [ ] 輸入正確的密碼
- [ ] 成功加入計畫
- [ ] 計畫列表中顯示該計畫
- [ ] 參與者數量變成 2 人
- [ ] 顯示兩個不同顏色的點

### 錯誤處理測試
- [ ] 加入計畫時輸入錯誤密碼 → 顯示「密碼錯誤」
- [ ] 加入計畫時輸入不存在的 ID → 顯示「計畫不存在」
- [ ] 創建計畫時不輸入名稱 → 顯示「請輸入計畫名稱」
- [ ] 創建計畫時不輸入密碼 → 顯示「請設定計畫密碼」

---

## 🐛 常見問題排查

### 問題 1: App 啟動後一直在載入
**解決方法:**
1. 檢查 Firebase 配置是否正確（.env 檔案）
2. 查看終端的錯誤訊息
3. 重新啟動 Metro Bundler

### 問題 2: 創建計畫後沒有顯示
**解決方法:**
1. 檢查網路連接
2. 查看 Firebase Console 確認資料是否寫入
3. 檢查終端的錯誤訊息

### 問題 3: 加入計畫失敗
**解決方法:**
1. 確認計畫 ID 正確（6 位數字）
2. 確認密碼正確
3. 檢查網路連接
4. 查看終端的錯誤訊息

### 問題 4: 編輯個人資料後沒有更新
**解決方法:**
1. 檢查是否點擊了「儲存」按鈕
2. 重新進入個人資料頁面
3. 查看 AsyncStorage 是否正常工作

---

## 📱 測試環境

- **開發伺服器:** http://localhost:8081
- **Metro Bundler:** 運行中
- **建議測試設備:** 
  - Expo Go (iOS/Android)
  - 或使用模擬器

---

## 🔍 調試技巧

### 查看 Console 日誌
在 Expo Go 中搖動設備，選擇「Show Dev Menu」→「Debug Remote JS」

### 查看 AsyncStorage
```javascript
import AsyncStorage from '@react-native-async-storage/async-storage';

// 在任何組件中添加
const checkStorage = async () => {
  const data = await AsyncStorage.getItem('@japan_trip_app:local_user');
  console.log('Stored user:', data);
};
```

### 查看 Firestore 資料
前往 Firebase Console → Firestore Database → trips collection

---

## ✨ 測試完成後

如果所有測試都通過：
1. ✅ 核心功能正常運作
2. ✅ 可以開始使用或繼續開發其他功能
3. ✅ 建議進行更多邊界情況測試

如果有問題：
1. 查看終端錯誤訊息
2. 檢查 Firebase Console
3. 使用 Debug Remote JS 查看詳細日誌
